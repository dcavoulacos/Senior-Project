<%= simple_form_for(@set_play) do |f| %>
  <% if @set_play.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@set_play.errors.count, "error") %> prohibited this set_play from being saved:</h2>

      <ul>
      <% @set_play.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div style="float:left;">
  <%= f.input :name, input_html: {style:"width:194px;"}%>
  <%= f.association :offense, collection: Offense.all, label_method: :name, include_blank: false %>
  </div>
  <!-- <canvas style="margin-left:50px;" id="court" width="500px", height="470px"></canvas> -->

  <div id"submit_button" class="clear"><%= f.submit %></div>
 
<% end %>
